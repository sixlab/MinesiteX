spring:
  application:
    name: msx-service-user
  profiles:
    active: msx-service-user
  cloud:
    config:
      uri: http://localhost:8500/
      name: application-msx-service-user,application-common-lib,application-common-service
      profile: prod
