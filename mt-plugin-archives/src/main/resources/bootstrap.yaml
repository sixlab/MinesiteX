spring:
  application:
    name: mt-plugin-archives
  profiles:
    active: mt-plugin-archives
  cloud:
    config:
      uri: http://localhost:8500/
      name: mt-plugin-archives
      profile: prod