spring:
  application:
    name: msx-base-zuul
  profiles:
    active: msx-base-zuul
  cloud:
    config:
      uri: http://localhost:8500/
      name: application-msx-base-zuul
      profile: prod
zuul:
  sensitive-headers: