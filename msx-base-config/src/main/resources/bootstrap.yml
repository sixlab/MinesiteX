server:
  port: 8500
spring:
  profiles:
    active: native,msx-base-config
  application:
    name: msx-base-config
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/, file:./config/
